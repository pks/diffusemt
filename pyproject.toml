[project]
name = "diffusemt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.6.0",
    "sacrebleu>=2.6.0",
    "torch>=2.10.0",
    "transformers>=5.2.0",
]
